package com.tfg.aegis.person.user.model;

public class Enums {
    public enum TypeRole {
        USER,
        ADMIN
    }

    public enum VerificationStatus {
        PENDING,
        VERIFIED,
        REJECTED
    }
}